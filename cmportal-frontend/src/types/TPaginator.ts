export type TPaginator = {
    before: number;
    current: number;
    first: 1
    last: number;
    limit: number;
    next: number;
    previous: number;
    total_items: number;
    total_pages: number;
}
